; Example: Loops in Ana
require "std/io".

; For loop (simulated with recursion, if Ana lacks native for)
ForLoop  (n) {
    if (n < 1) {
        return Unit.
    }
    ioOutln ["For loop iteration:", n].
    ForLoop [n - 1].
}.

ForLoop [5].

; While loop (simulated with recursion)
WhileLoop  (n) {
    if (n < 1) {
        return Unit.
    }
    ioOutln ["While loop iteration:", n].
    WhileLoop [n - 1].
}.

WhileLoop [3]. 